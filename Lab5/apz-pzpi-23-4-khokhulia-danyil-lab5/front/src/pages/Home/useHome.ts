export function useHome() {
  // Currently, the Home page has no complex state or API calls, 
  // but this file is prepared for future business logic (e.g., fetching featured laundries).
  return {};
}
